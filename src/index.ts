/**
 * Public entry point for the unified message data model.
 *
 * Exports the Zod schemas (runtime validation) and their inferred TypeScript
 * types under the same names, plus a few parse/validate helpers.
 */
export * from "./model.js";
